pip install transformers torch flask nltk

from flask import Flask, request, jsonify
from transformers import GPT2LMHeadModel, GPT2Tokenizer
import nltk

# Flask app initialize karo
app = Flask(__name__)

# Model aur tokenizer load karo
model = GPT2LMHeadModel.from_pretrained("gpt2")
tokenizer = GPT2Tokenizer.from_pretrained("gpt2")

# Text generation function
def generate_response(input_text):
    inputs = tokenizer.encode(input_text + tokenizer.eos_token, return_tensors="pt")
    outputs = model.generate(inputs, max_length=1000, pad_token_id=tokenizer.eos_token_id)
    response = tokenizer.decode(outputs[0], skip_special_tokens=True)
    return response

# Flask route jo POST request lega aur response return karega
@app.route("/chat", methods=["POST"])
def chat():
    user_input = request.json["message"]
    response = generate_response(user_input)
    return jsonify({"response": response})

# Flask app ko run karo
if __name__ == "__main__":
    app.run(debug=True)

  transformers==4.28.0
torch==1.13.1
flask==2.2.2
nltk==3.7

  AI-Chatbot/
├── app.py           # Main Flask app
├── requirements.txt # Required dependencies
├── README.md        # Project description (optional)


  python app.py

  {
    "message": "Hello, how are you?"
}
{
    "response": "Hello! How can I help you today?"
}
